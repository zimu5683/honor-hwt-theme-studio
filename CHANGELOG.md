# 更新记录

## 未发布

## 0.1.6.3 - 2026-08-05

- Windows 发布改为关闭 UPX 的 PyInstaller onedir ZIP，避免单文件自解压载荷触发 Defender 启发式拦截。
- 自动更新优先下载 ZIP，完成 SHA-256 校验后安全解包，并拒绝路径穿越、重复路径、符号链接和超大归档。
- 发布与测试流水线增加便携目录结构校验，保留旧版 EXE 更新兼容。

## 0.1.6.2 - 2026-08-05

- 华为/荣耀资源映射改为按资源键和包名前缀边界匹配，避免 `myhwvalue`、`com.huawei2` 等普通名称被误改写；已确认的 `emui`、`hw`、框架目录和包名映射保持兼容。
- 源主题兼容性报告增加有界的华为/荣耀映射审计，统计模块、资源键和图标 fan-out 的最终目标，并保留最多 256 条样本，导出前即可发现映射范围。
- Android SAF 切换 Honor/Themes 目录时会在新授权成功持久化后释放旧 tree URI，避免反复选择目录积累
  持久化读写权限；新目录验证或保存失败时仍保留旧授权。
- 手机自动发现参考 LocalSend 的 HTTP legacy fallback：UDP 没有结果时，仅对活动网卡推导的私有
  IPv4 小网段进行最多 256 个有界状态探测，并限制并发、单请求超时和总发现预算；公共地址、
  超大网段、非法响应不会进入设备列表。
- 新增后续参考项目路线文档，记录华为主题转换、LocalSend 协议、资源管理和 ZIP 安全方向的借鉴重点。
- 导出目标规划统一处理华为/荣耀映射后的重复路径：同内容资源合并，内容不同且来源可判定时荣耀原生资源优先，所有决策写入导出报告，无法安全判定的自定义冲突仍阻止导出。
- 手机传输增加可选的 LocalSend 风格上传会话标识和取消接口；桌面端取消时通知支持该扩展的手机，Android 在安装提交前确认会话未被取消，同时保留 Bearer v1 PUT 兼容路径。
- 手机传输在会话标识基础上增加一次性幂等重试；连接在提交响应前断开时复用同一 SHA-256 会话，Android 最近成功结果最多缓存 8 项，避免重复安装同名主题。
- 手机传输增加可选的会话状态查询；连接断开后桌面端会优先确认 `completed`/`receiving`/`not_found` 状态，旧版助手仍直接回退到 Bearer PUT 重试。
- 手机传输的分块与恢复状态现在严格校验 `transfer_id`；Android 完成响应同步回显会话标识，避免跨会话进度被误用。
- 资源扫描增加源主题兼容性报告，分类记录重复资源、图片格式错配和非标准 XML，并明确与导出 HWT 的严格验证分离。
- ZIP 验证在读取外层和嵌套内容前先拒绝路径穿越条目，减少恶意归档的无谓读取并收紧不可信输入边界。
- 导出扫描到的华为资源时自动应用已验证的荣耀模块、框架目录、XML 资源名和 PNG 文件名映射，并在报告中使用最终荣耀目标路径。
- 主题扫描与最终验证共用 ZIP 安全路径规则，扫描阶段也会跳过外层和嵌套路径穿越条目，避免恶意资源进入目录缓存。
- Android 配对电脑名称会清理控制字符、折叠空白并按 Unicode 代码点截断，避免异常名称污染界面或截断代理字符。
- Android 接收服务在长时间上传或其他活动请求期间不会被空闲超时任务中断，并限制配对 JSON 请求体为 16 KiB。
- Android SAF 探测文件使用随机名称并在失败路径清理；同名主题替换失败时恢复备份，避免误删最终文件。
- Android SAF 权限探测即使写入失败也会清理临时探测文件，清理失败会阻止保存无效授权状态。
- Android SAF 权限状态读取遇到撤权或 Provider 异常时关闭失败，不会让服务启动或主界面因权限检查异常退出。
- HWT 工程重复保存前会清理同一进程遗留的临时 staging 文件，避免上次中断的目录污染下一次保存。
- HWT 扫描和验证对外层/嵌套 ZIP 增加条目与累计解压预算，在 CRC 检查和读取前拒绝压缩炸弹式超大内容。
- HWT 默认导出文件名对工程名过滤控制字符并限制长度，避免异常工程名生成不可用路径或污染界面文本。
- 默认导出名在长度截断后继续清理末尾句点和空格，避免生成 Windows 不接受的文件名。
- 自动更新资产名限制为 200 个 UTF-8 字节，超长 Release 文件名会在创建缓存路径前拒绝。
- Android 全盘主题替换记录原文件是否已成功移动到备份，备份移动失败时不会把空备份误恢复到原路径。
- Android SAF 安装会在互斥锁内固定授权 Uri，授权切换、空间检查和最终落盘不会在一次传输中串到不同目录。
- Android 主题替换拒绝同名目录、特殊文件和符号链接；SAF 恢复与残留清理也只处理普通文件，避免把目录误当作主题移动或删除。
- Android 全盘主题备份名绑定目标主题并在下次安装前恢复中断状态，同时清理可确认的残留上传文件；无法映射的旧备份不会被误删。
- Android SAF 安装失败时检查正式文件清理结果，并重新查询确认备份已恢复为普通文件，避免回滚失败后把半成品留在主题名下。
- Android SAF 改名遇到 Provider 返回失败或改名后查询短暂异常时按实际文件对象重新确认并回滚，避免误删已发布文件或遗留原主题备份。
- Android SAF Provider 改名抛出异常时也会重新核对源/目标状态；已完成的改名继续提交，明确未改名的 Provider 仍可回退到安全复制路径，状态不明确则失败关闭。
- 用户资源目录与兼容性报告事务增加线程及跨进程锁；读取时遇到锁竞争回退内置目录，写入不会在并发进程间互相覆盖事务记录。
- Android JVM 单元测试加入真实 JSON 实现，避免 Android SDK 桩方法掩盖协议解析回归。
- Android 接收服务停止或重启时清理分块会话状态与孤儿 `.uploading` 文件，避免 LocalSend 风格取消后的缓存残留。
- ZIP 结构审计增加文件/目录前缀重叠检测，外层和嵌套模块在读取前拒绝有歧义的归档路径。
- Android HWT 预检同步拒绝文件/目录路径重叠，保持桌面验证器与手机接收边界一致。
- 桌面端限制手机响应文本字段长度并清洗远端错误文案，超长或含控制字符的 profile/状态字段会拒绝，错误日志保持单行有界。
- 手机配对记录的端口和协议版本只接受 JSON 整数，profile 的 SDK 字段不再把字符串/浮点值静默转换成有效版本。
- Android 配对记录时间戳只接受 JSON 的 Int/Long 整数，拒绝小数或其他 Number 转换，避免不同 JSON 实现产生不一致。
- 手机记录中的设备名、主机、令牌和应用列表增加单字段边界；异常核心字段会丢弃设备或令牌，超长展示字段不会撑大界面。
- 手机传输协议文档同步记录 HWT ZIP 预算、SAF 目标固定和远端文本字段边界，避免实现与说明漂移。
- 工程 JSON 打开增加 16 MiB 有界读取和顶层结构校验，损坏或异常大的工程会以明确错误失败，不再把底层类型异常带到界面。
- Android 导入和接收 HWT 时检查 ZIP 单条目与累计解压预算，超过 256 MiB/条目或 512 MiB 总量的压缩炸弹会在落盘前拒绝。
- 资源目录缓存增加 32 MiB 有界读取和记录结构校验，损坏缓存继续回退到内置目录，不会阻塞启动。
- 保存工程会按当前图片引用重建 `.assets`，清理已删除修改留下的无用文件，同时保留禁用图片以便之后重新启用。
- 桌面自动更新为每个任务使用独立临时文件，取消、校验失败或提交异常都会清理残留下载文件。
- 目录持久授权失败或验证取消时不会误保存或撤销已保存的有效权限；设备信息子进程会显式回收。
- 配对记录和手机配置响应增加容错清洗，损坏或旧版本数据不会阻塞桌面端启动和识别手机。
- 手机主题上传增加文件一致性与严格长度保护；Android SAF 替换同名主题前先备份并在最终哈希校验后提交。
- Android 接收助手在可用空间为零或不足时失败关闭，并为临时文件保留 16 MiB 余量。
- 自动更新链路统一要求 HTTPS，拒绝明文清单、安装包和校验文件地址。
- 主窗口关闭时会取消更新、手机识别、局域网发现和传输任务；后台线程结束后再释放窗口，避免 Qt 线程残留。
- 更新检查、安装包校验和 Termux/SSH 子进程支持取消，关闭窗口或取消操作不会继续占用后台任务。
- Android 停止接收时立即释放 HTTP、UDP 和超时任务，并移除前台通知，避免快速重启时残留端口或状态。
- 桌面更新、手机识别和传输任务结束后安排 QThread 对象自删除，重复操作不会持续累积已结束线程对象。
- 配对记录写入增加同路径并发锁和唯一临时文件，多个后台任务同时发现或更新手机时不会互相覆盖记录。
- 手机记录跨进程锁增加 5 秒竞争超时，异常占锁不会无限挂起发现、配对或窗口关闭流程。
- 手机发现、状态和配对响应增加字段类型与端口校验；Android 异常请求统一返回通用 500 文案，不泄露内部异常细节。
- 桌面端严格校验手机上传成功响应的 SHA-256、文件大小和布尔字段，异常响应不会被误报为发送成功。
- 桌面手机识别对未知错误 code 的远端文案做单行压缩和长度限制，异常响应不会撑大消息框或注入多行界面文本。
- UDP 手机发现同样校验设备标识、端口和字段类型，异常广播不会污染设备列表或配对记录。
- 自动更新复用已有安装包时也支持逐块取消，关闭窗口不会先完整哈希大缓存文件。
- HWT 扫描和验证统一使用禁止外部 DTD、实体展开及网络访问的 XML 解析器，降低恶意主题文件触发外部实体读取的风险。
- Android 本地导入与网络接收共用安装互斥，避免并发写入同名主题时争用 `.uploading` 临时文件。
- Android 接收状态改用原子 StateFlow 更新，避免网络线程、超时任务和界面线程并发刷新时丢失字段。
- Android 配对记录改为逐条容错解析，单个损坏客户端不会导致其他有效令牌被新配对覆盖。
- 桌面真机预览改用 RGBA 内存直传 Qt，避免启动时反复 PNG 编码/解码；资源表缓存中文标签并跳过无变化的重复重置，降低启动耗时。
- 手机发现与 HTTP 响应拒绝非对象 JSON、非法 Content-Length、非整数协议字段和错误 profile 类型，避免异常响应中断发现或污染已保存设备状态。
- Android SAF 空间检查按授权 tree 对应的存储卷执行，并在安装开始时固定 SAF/全盘目标，避免权限竞态时误写另一目录。
- Android 配对请求严格解析 JSON 字段并将格式错误映射为 400；主题落盘成功后，即使状态通知回调失败也不会向电脑误报上传失败。
- Android 全盘写入同名主题前使用随机备份并在最终哈希校验失败时恢复；SAF 改名失败的备用写入会先清理临时文件，避免短时占用两份 HWT 空间。
- Android 主题安装回滚只删除本次安装实际发布的文件对象，并发出现同名文件时不会按路径误删其他文件。
- Android 服务启动和本地导入对底层异常使用通用提示，仅保留受控传输错误文案，避免系统 Provider/端口细节直接展示。
- Android 拒绝通知权限后不再继续启动后台接收服务，避免无效前台服务和延迟错误状态。
- Android 主题安装的文件系统和 SAF 权限异常统一映射为可恢复的存储不可用错误，避免向电脑暴露内部异常文本。
- 主窗口启动更新检查使用关闭状态守卫；窗口关闭后，延迟定时器和后台回调不会重新启动更新任务或触碰已释放的界面。
- 桌面导出和发送主题时记住最近使用的目录，并按系统文档、下载目录和用户主目录回退，移除对固定 Windows 盘符的依赖。
- 手机发现对话框为每次 UDP 搜索绑定线程身份；旧搜索的完成信号不会清空新搜索，关闭对话框时也会忽略迟到的发现结果和错误回调。
- 手动连接支持 IPv6 主机和标准的 `[地址]:端口` 写法，设备列表对 IPv6 地址使用方括号显示，并即时拒绝非法端口。
- 手动连接对裸 IPv6 和方括号 IPv6 使用标准地址解析，格式错误的多冒号输入会在连接前拒绝。
- 桌面端严格比较手机响应的 `Content-Length` 与实际读取字节数，截断或伪造长度的响应会在 JSON 解析前拒绝。
- 桌面上传在发送完本地文件后再次校验文件签名，发送过程中被替换或修改的主题不会继续等待并误报成功。
- 自动更新的 Release 清单和 SHA-256 文本使用统一有界读取，超限、非法长度或截断的元数据不会进入版本解析。
- 自动更新下载严格比较安装包 `Content-Length` 与实际字节数，非法或截断响应会清理 partial 文件并拒绝提交。
- 自动更新资产名拒绝控制字符和首尾空白，避免 Windows 文件名与更新提示中的名称错位。
- 自动更新发布说明和版本字段限制展示长度，超长 release 文本不会直接撑大更新提示框。
- 已保存的手机记录拒绝布尔类型端口，避免 Python 的整数子类规则把损坏数据转换成端口 `1`。
- 已保存手机 profile 只接受字符串展示字段、非负 SDK 和字符串应用包名，并丢弃不兼容协议版本，避免损坏记录被转换后继续使用。
- 已保存的手机记录对设备 ID、主机、令牌和协议版本执行严格类型清洗，异常核心字段整条丢弃，可选展示字段仍使用安全默认值。
- Android 空闲自动停止现在复用完整停止流程，先释放 HTTP/UDP/计时任务并更新界面状态，再停止服务；空闲边界和活动请求判断增加独立测试。
- Android 接收服务为空闲计时任务增加生命周期 generation 和串行清理，快速重启时旧 timeout 不会关闭新一轮 HTTP/UDP 服务。
- Android 刷新配对码与接收服务停止共用生命周期锁，停止竞态不会把已停止界面重新发布成运行状态。
- Android 停止接收时先摘除资源引用，再分别清理 HTTP、UDP 和前台通知；单项 stop 异常不会阻断剩余状态收尾。
- Android 上传请求将负数 `Content-Length` 正确返回为 `400 invalid_length`，仅超过 1 GiB 才返回 `413 too_large`。
- 桌面更新、手机识别和主题传输回调携带任务 generation，旧线程的结果、进度和完成信号不会清理或更新新任务的界面。
- 手机发现结果与完成回调改为 GUI 线程 queued receiver；停止超时后会等待线程真正结束再关闭对话框，避免销毁仍运行的 Qt 线程。

## 0.1.6.1 - 2026-08-03

- 修复启动更新检查在错误线程显示对话框，导致检查更新卡顿或窗口无响应的问题。
- 启动时检查更新改为静默，只有发现新版本时才弹出提示；手动检查仍显示检查结果。
- 修复 Android UDP 自动发现服务快速停止后无法再次启动，以及 socket 和后台线程未完全收尾的问题。
- Windows 桌面端与 Android 手机助手版本同步更新至 `0.1.6.1`，Android `versionCode` 更新为 9。

## 0.1.6 - 2026-08-03

- 简洁编辑加入基于 ELP-AN00 / MagicOS 10.0.0 隐私空间授权真机截图的 13 个场景，覆盖全部 30 个项目；缩略图标出受影响位置。
- 点击真机缩略图可查看“原始参考 / 当前设置预览”对比，颜色支持 `#AARRGGBB` 透明混合，图片复用裁剪、完整放入、拉伸、取景和明暗增强。
- 创建窗口前加载 Qt 中文翻译，颜色窗口、确认框、输入框和标准按钮使用中文；高级资源类型改为中文显示并保留英文内部值。
- 新增可复现的 ADB 采集与素材整理工具；原始截图/XML 与发布素材隔离，EXE 只打包隐私空间授权的缩放参考图。
- Windows 桌面端版本更新至 `0.1.6`，HWT 工程、导出格式、撤销记录、手机助手协议和 Android APK 保持兼容。

## 0.1.5.1 - 2026-08-02

- 将 Windows 和 Android 界面从 IBM Carbon Light 重构为 Studio Soft Light：暖灰画布、紫色主操作、白色圆角卡片和低饱和度类别色块。
- Windows 新增自定义客户端标题栏，支持拖动、双击最大化、最小化、还原、关闭和边缘缩放；“高级”操作收进工具栏“更多”菜单。
- 简洁编辑卡片改为 1200/720px 断点下的 3/2/1 列布局，并增加更大的图片预览区域和可折叠主题信息卡片。
- Android 传输助手新增 StudioSoftTheme、淡紫欢迎区域、圆角状态卡片和语义状态色；协议、文件格式与传输流程保持兼容。
- 桌面与 Android 版本更新为 0.1.5.1，Android `versionCode` 更新为 7。

## 0.1.5 - 2026-08-02

- 修复 Windows 单文件发布包被 Microsoft Defender 启发式误报的问题：PyInstaller 构建关闭 UPX 压缩，并同步更新发布校验文件。
- 保留 IBM Carbon Light 桌面端、Compose Android 助手和主题工程格式兼容性；Android `versionCode` 更新为 6。

## 0.1.4 - 2026-08-02

- 按根目录 `DESIGN.md` 将 Windows 桌面端统一为 IBM Carbon Light：集中式 PySide6 QSS、IBM Plex Sans SC、IBM Blue 语义色、方角和 1px hairline，无阴影。
- 简洁编辑按 1312/672px 断点重排为 4/2/1 列；高级编辑筛选栏和资源详情面板在窄窗口自动改为纵向布局。
- 统一 primary、secondary、tertiary、ghost、danger 按钮角色，以及 success、warning、error、info 状态反馈。
- Android 传输助手改为 Jetpack Compose Carbon Light 主题：自定义 ColorScheme、Typography、Shapes，移除默认 Material 圆角和 elevation，并在 672dp 处切换单栏/双栏布局。
- 将 IBM Plex Sans SC 字体和 `docs/design-md/awesome-design-md/` 设计资料纳入发布资源；桌面与 Android 版本同步至 0.1.4。
- Windows 发布构建关闭 UPX 压缩，降低 PyInstaller 单文件被 Defender 启发式误报为 `Trojan:Win32/Sabsik.TE.A!ml` 的概率。

## 0.1.3 - 2026-07-26

- 默认使用 30 项纯中文“简洁编辑”，将数千个荣耀、Android、华为兼容资源自动合并处理。
- 新增按主题、全局外观、系统界面、桌面和常用应用组织的卡片式编辑，以及分组修改记录。
- 高级编辑默认隐藏包名、资源名和路径，并改进资源作用的中文说明。
- 手机助手可在配对后返回型号、MagicOS/Android 版本和主题相关应用安装情况，用于自动精简界面。

## 0.1.2 - 2026-07-26

- 桌面端应用全新的浅蓝色 Material 视觉主题，统一窗口、菜单、标签页、表格和表单控件样式。
- 工具栏新增功能图标，并优化按钮、输入框、分组卡片、焦点和悬停状态。
- 新增 `qt-material` 与 `qtawesome` 运行依赖，Android 传输助手版本同步至 0.1.2。

## 0.1.1 - 2026-07-26

- 新增 Android 13–16“荣耀主题传输助手”APK，可通过 SAF 或全盘权限备用方式写入 `Honor/Themes`。
- 桌面端新增 UDP 自动发现、6 位码配对、令牌持久化、流式上传、实时进度和取消。
- 传输使用 SHA-256 双端校验、临时文件写入和同名覆盖，避免留下损坏主题。
- 原有 Termux/SSH 发送移入高级备用流程。
- GitHub Actions 同时测试并发布 Windows EXE、签名 APK 与 SHA-256 文件。

## 0.1.0 - 2026-07-26

- 防止导出覆盖资源目录对应的原始主题，并正确保留未保存工程状态。
- 工程升级为可移植格式，自动收集图片并支持缺失图片恢复和灰白占位图。
- 增强自定义资源、重复目标、ZIP、XML、数值和图片格式验证。
- 改进重新扫描后的完整界面刷新、搜索性能、导出摘要和手机连接预检。
- 拆分主要 UI 组件，增加 GUI/回归测试、Windows 构建检查和标签自动发布。

## 0.0.2 - 2026-07-25

- 修复导出的 HWT 不出现在荣耀“主题”本地列表的问题。
- 增加主题管理器必需的 `preview/` ZIP 目录项和空 `icons` 兼容模块；未配置图标仍使用系统默认。
- 验证器新增荣耀本地主题识别条件。
- SSH 发送后自动打开荣耀“主题”应用并提示刷新路径。

## 0.0.1 - 2026-07-25

- 首个公开测试版本。
- 从“大雪”HWT 构建只读资源目录和最小空白主题。
- 提供颜色、图片、NinePatch、壁纸、图标和高级资源编辑。
- 提供按需 HWT 导出、完整性校验和 `phone-termux` SSH 发送。
- 内置 12,904 个资源槽位的目录。
